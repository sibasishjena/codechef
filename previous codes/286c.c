#include<stdio.h>
#include<stdlib.h>

int main()
{
	int n,d,p[100000],i;
	scanf("%d %d",&n,&d);
	for(i=0;i<n;i++)
	{
		scanf("%d",&p[i]);
	}
	int count=0;
	for(i=0;i<n;i++)
	{
		if(d==p[i])||(d==p[i])||(d==p[i])
	}
return 0;}
